!!!

%html
  %head
    %meta{'charset' => 'utf-8'}
    %meta{'http-equiv' => 'x-ua-compatible', 'content' => 'ie=edge'}

    %title= t('app_title')

    %meta{'name' => 'description', 'content' => t('app_description')}
    %meta{'name' => 'viewport', 'content' => 'width=device-width, initial-scale=1'}

    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = javascript_include_tag 'modernizr'
    = csrf_meta_tags

  %body


    - flash.each do |key, value|
      = content_tag :div, content_tag(:p, value), id:"#(key)" unless value.blank?
      /for each flash message
      / oyutput a div containing %p
      / p has the css id as 'alert' or 'notice'

    %section

      %div{:id=>"flash"}
        #flash
          - flash.each do |key, value|
            %div{:class => "flash #{key}"}= value



      = render 'shared/header'

      /alerts the user of a message from devise 
      %p.notice= notice
      %p.alert= alert
      
      / yield tag covers all view pages
      = yield

      = render 'shared/footer'
