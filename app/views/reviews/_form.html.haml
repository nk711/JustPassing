= simple_form_for([@profile, @profile.reviews.build]) do |f|
	= f.input :rating ,label: false, placeholder: "rating"
	= f.input :comment ,label: false, placeholder:  "Write a comment!"
	= f.button :submit, label: t('.submit')
